[project]
name = "mycelium-memoirs"
version = "0.1.0"
description = "Creative pipeline: spoken experience reports → stories, image prompts, and video prompts."
requires-python = ">=3.10"
dependencies = [
    "faster-whisper>=1.0.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]
